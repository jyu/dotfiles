set number "Show line numbers    
set cursorline "Highlight current line
set colorcolumn=81 "Highlight column #81 in your editor so you know to avoid long lines
set tabstop=2 "4-column tabs
set shiftwidth=2 "4 space shift (the '>' and '<' commands)
set softtabstop=2 "4 space tabs when using spaces
set expandtab "Use spaces instead of tabs (use 'set noexpandtab' to use hard tabs)
set autoindent "autoindent on new lines

colorscheme desert

syntax on
